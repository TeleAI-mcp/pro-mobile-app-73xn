name: Auto Close Issues

on:
  issues:
    types: [labeled]

jobs:
  auto-close-issues:
    runs-on: ubuntu-latest
    if: |
      contains(github.event.label.name, 'wontfix') ||
      contains(github.event.label.name, 'completed')
    permissions:
      issues: write
    steps:
      - name: Close issue
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.update({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'closed'
            })